# Copy this file to data\db.env.ps1 and edit the values.
$env:PGHOST = "localhost"
$env:PGPORT = "5432"
$env:PGDATABASE = "forhome"
$env:PGUSER = "postgres"

# Optional. Leave unset if you use pgpass, trust auth, or interactive auth.
# $env:PGPASSWORD = "your-password"
